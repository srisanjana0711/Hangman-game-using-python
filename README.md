# Hangman-game-using-python

#import time module
#set a word which the player has to guess
#here we set the word as secret
#set number of turns(here 10)
#now use a loop 
#And check whether the letter is present in the word or not
#if so print the words in specific place and decrement turn
#else decrement turns
#if user find all the words correctly within given turn,print "win"
#otherwise print "lose"


